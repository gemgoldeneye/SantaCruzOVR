/**
 * Re-export barrel: the canonical OVR domain model now lives in the SDK package
 * `@gelabs/ovr/types`. Kept here so existing `@/lib/types` imports resolve
 * unchanged during the extraction.
 */
export * from "@gelabs/ovr/types";
